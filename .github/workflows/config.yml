        with:
          username: venkateshdukuru

name: Example Workflow

on:
  push

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: Use action from the same repository
        uses: ./path/to/local/action

      - name: Use action from Docker image
        uses: docker://docker-image-name

      - name: Use action from your repository
        uses: venkateshdukuru/movie-recommender-system@main
        with:
          username: venkateshdukuru